# 2026-09-24 量化实验室 V0

授权增加一条 Qlib 研究基线，作为 0.2.0「不扩功能」停止线的单独例外。真实持仓、资产、信息流和问答不改。

采用 Python 3.11 独立虚拟环境、PyPI `pyqlib==0.9.7`、LightGBM CPU。数据来自已配置的 Tushare，股票池是固定教学小样本，基准是沪深 300 指数行情。页面只在本机启动预登记任务并读取实验摘要。

2026-09-25 做了 V0.1 校准：Qlib 价格改为未复权价乘以 `adj_factor`，成交量改为原始股数除以 factor，`$change` 用未复权涨跌幅。收益摘要改为扣费前是 `return`、扣费后是 `return - cost`，并单独给出账户区间收益。新数据版本是 `v0.1-ef33b0d00be1`，新实验 `d4e5f6a7-b8c9-4d0e-8f12-3456789abcde` 不覆盖旧实验。模型在第 1 轮早停，测试日不同分数的中位数是 3。
