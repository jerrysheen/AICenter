# 持仓手动刷新行情

## 目标

持仓页打开后不再自动拉价，且行情端口有 20 秒缓存。加一个可点的刷新，方便核对是缓存、页面没重拉，还是源站延迟。

## 决定

- `GET /api/v1/holdings?refresh=1` 经 Contract 解析后交给 Trading Service。
- 行情端口在 `refresh` 时跳过 TTL 缓存；报价改用 1 分钟粒度，默认仍是 5 分钟。
- 页面「刷新行情」带当前报价时间。价格不变、时间在走，更可能是源站延迟。

## 验证

- `npm run check`
- 浏览器持仓：点刷新后说明里的报价时间更新。
