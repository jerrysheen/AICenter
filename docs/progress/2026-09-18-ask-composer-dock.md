# 2026-09-18 问答输入贴底，点提问即落焦

## 目标

对照豆包回复窗：问答输入贴在键盘上方，点「新提问」或右上角 + 时直接进入输入框。

## 决定

- 会话层输入条固定在底栏之上；键盘弹出时抬升并暂时让底栏让位。
- 去掉输入框外圈蓝色描边，改成浅灰圆角回复条。
- 不改问答 Contract / Worker；联网模式仍是显式选择。
- 打开已有会话不自动弹键盘，只有新提问才落焦。
