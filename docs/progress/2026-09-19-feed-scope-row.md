# 信息流信源入口提到同一行

## 目标

X / B站不再只藏在筛选弹层里。社媒拉取入口不再「先选平台，再点更多」。

## 决定

- `全部 / 重点 / 社媒 / 官方 / X / B站` 共用频道栏，横向滑动。
- 右侧始终是同一个折叠按钮：范围页打开筛选；X / B站打开该信源任务。
- 筛选弹层保留，仍可改排序、日期和手工平台。

## 验证

信息流频道栏能滑到 X / B站。切到 X 后右侧仍是折叠图标，点开可拉取时间线。回到「全部」后同一按钮打开筛选。
