# 阅读页操作栏对齐，正文状态可打开原文

日期：2026-09-19

## 目标

阅读页底部「存入灵感 / 引用到问答 / 星标」图标大小一致；元信息里的「正文完整 / 仅索引」在有来源链接时能点出去。

## 决定

- 操作栏 SVG 统一 21px，与现有星标一致；文字按钮均分剩余宽度，星标保持 `icon-button` 固定宽。
- 「正文完整 / 仅索引」有 `sourceUrl` 时走已有 `appendExternalLink` / `openExternalHttpUrl`，不新开入口。

## 验证

阅读页三个图标同高；点「正文完整」新窗口打开原文。
