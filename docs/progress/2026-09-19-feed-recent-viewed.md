# 信息流增加最近浏览

## 目标

看过的条目单独留一份，避免已读下沉或从其他栏目删除后找不到。

## 决定

- 频道栏增加「最近」，总览时间线同步。
- 打开详情即写入本机 `recent-items` 副本，最多 80 条，按浏览时间排列。
- 其他栏目删除不影响最近；最近里左滑删除只去掉这份浏览记录。
- 已读半透明仍只在重点。

## 验证

打开一条后「最近」能看到。再从社媒或全部删除，最近仍在。最近里删除后，其他栏目不受影响。
