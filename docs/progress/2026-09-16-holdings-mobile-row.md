# 持仓列表移动端首屏改密

## 目标

手机上持仓行中间一大块空白，要横滑才看得到当日盈亏。

## 决定

- iOS `button` 去掉系统默认外观，避免行上出现方块底。
- 窄屏不再用 760px 横滑表：名称、现价/成本、当日盈亏三列，行高压到约 52px。
- 持仓数放到名称下面，和市值同一行；当日盈亏首屏同时显示涨跌幅和金额。
- 所有 `button` 去掉 WebView 默认灰底；底栏和筛选芯片不再出现方块底。
- 持仓盈亏仍在点进详情里看。宽屏保持原多列表头。
